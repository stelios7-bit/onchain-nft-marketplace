{
  "name": "onchain-nft-marketplace",
  "version": "0.1.0",
  "description": "Marketplace for ERC-721 and ERC-1155 assets with no-escrow sales and ETH/WETH/ERC-20 payment",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat deploy",
    "deploy:localhost": "hardhat deploy --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17",
    "hardhat-deploy": "^0.12.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
